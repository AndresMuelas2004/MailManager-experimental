VITE_API_BASE_URL=
VITE_GOOGLE_CLIENT_ID=
VITE_MICROSOFT_CLIENT_ID=
# Local dev only: set to "true" to auto-login through the backend dev-login
# backdoor and skip the login screen. Ignored in production builds
# (gated by import.meta.env.DEV). Requires DEV_LOGIN_ENABLED in the backend.
VITE_DEV_AUTO_LOGIN=
