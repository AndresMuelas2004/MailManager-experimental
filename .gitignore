# IDE
.idea/
.vscode/

# Claude Code local config
.claude/settings.local.json
.claude/.memory-write-flags/
.mcp.json

# Worktrees que crea el propio Claude Code (subagentes `isolation: worktree`, sesiones en
# segundo plano, `claude --worktree`, sesiones paralelas del escritorio). Sin esta entrada
# aparecen como ficheros sin trackear y abortan las comprobaciones de árbol limpio de
# /creacion-worktree (G2), /shareChanges (fase 1) y /ship (4.1).
.claude/worktrees/
claudePlans/
.playwright-mcp/

# Virtual environment
.venv/

# Python cache / compiled files
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/

# Test coverage
.coverage
htmlcov/

# Playwright / E2E artifacts
test-results/

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Environment variables / secrets
.env
.env.*
!.env.example
!.env.production.example
backend/.env
backend/.env.*
!backend/.env.example
!backend/.env.docker.example
frontend/.env.local
frontend/.env.*
!frontend/.env.example

# OAuth credentials
credentials/

# Personal notes
PersonalNotes.md


# Local scripts / experimental code
backend/Scripts/*
!backend/Scripts/CLAUDE.md
!backend/Scripts/cli_utilities/
backend/Scripts/cli_utilities/__pycache__/

# Runtime persisted data (local only)
backend/data/

# Frontend
node_modules/
frontend/dist/

#Personal things
Ignore/
TldrawDiagrams/
UtilidadesDelProgramador/

# Browser automation sessions
.browser-sessions/

# External API documentation cache (generated by external-api-docs-researcher)
externalAPIinformation/
bug-analisis/

# Investigacion local exhaustiva de las APIs externas usadas (Gmail / Outlook) — no se versiona
external-apis-used/

# Skill output: features missing in the app
features-found-needed-implementation/

# Skill output: in-progress feature implementation plans (planear-implementacion-funcionalidad)
nueva-implementacion-en-curso/

# Competitive research output: Superhuman features missing in the app
Superhuman-features/

# Per-worktree compose overrides and seed data (generated by /creacion-worktree)
compose.override.yml
compose.override.yaml
/initdb/

# Informes locales de errores pendientes de corregir (no se versionan)
errores-pendientes-de-corregir/

# Mapa local de cuentas propietarias de consolas externas (Azure/Google) — no se versiona
cuentas-de-la-aplicacion.md
